load("@rules_shell//shell:sh_binary.bzl", "sh_binary")

sh_binary(
    name = "clang-tidy",
    srcs = ["run-clang-tidy.sh"],
    data = ["//voxeloo:clang-tidy-config"],
    tags = ["no-sandbox"],
    visibility = ["//visibility:public"],
)
