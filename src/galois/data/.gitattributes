*.blend filter=lfs diff=lfs merge=lfs -text

# Keep the Chapter 1 animation and generated combat source assets out of LFS
# while the repository's LFS budget is exhausted.
animations/character-animations.blend -text -filter -merge -diff
npcs/indisworm.blend -text -filter -merge -diff
npcs/*_animations.blend -text -filter -merge -diff
projectiles/harthmere_boss_attack_shapes.blend -text -filter -merge -diff
projectiles/harthmere_premium_projectiles.blend -text -filter -merge -diff
weapons/harthmere_premium_weapons.blend -text -filter -merge -diff
