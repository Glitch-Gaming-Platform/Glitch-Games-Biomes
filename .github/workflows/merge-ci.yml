name: merge-ci

# Manual CI entrypoint. Main branch pushes deploy through the Azure production
# workflow directly so only one paid workflow starts for a production deploy.

on:
  workflow_dispatch:

permissions:
  id-token: write # Needed for gcloud auth.
  contents: read

jobs:
  ts-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
      - name: git lfs pull
        uses: ./.github/actions/cached-lfs-pull
      - name: yarn install
        uses: ./.github/actions/cached-yarn-install
        with:
          token: ${{ secrets.BIOMES_DEPENDENCY_GITHUB_TOKEN }}
      - name: bazel setup
        uses: ./.github/actions/bazel
      - name: pip install
        uses: ./.github/actions/cached-pip-install
      - name: Restore eslint cache.
        uses: ./.github/actions/cached-eslint
      - run: ./b lint ts
      - run: ./b test
